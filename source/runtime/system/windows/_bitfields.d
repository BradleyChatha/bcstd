module runtime.system.windows._bitfields;

// Because dstep has a hard dependency on Phobos for its output, I need to reimplement
// bitfields enough for things to work...
//
// In the future! For now we'll just ignore bitfields until I need them.

string bitfields(Params...)()
{
    return "";
}